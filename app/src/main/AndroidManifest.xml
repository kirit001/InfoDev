<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.survey_application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Database.SurveyApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResetPassword.PasswordResetActivity"></activity>
        <activity android:name=".BottomNavigation.SurveyList.SurveyListEditActivity" />
        <activity android:name=".BottomNavigation.SurveyList.SurveyListActivity" />
        <activity android:name=".BottomNavigation.Survey.PersonalInfoActivity" />
        <activity android:name=".BottomNavigation.Survey.QualificationActivity" />
        <activity android:name=".BottomNavigation.Profile.ProfileActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".SplashLogin.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>